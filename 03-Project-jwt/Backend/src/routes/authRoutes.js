import express from 'express';

const router = express.Router();
// ruta POST pra registrar un usuario
router.post('/register', xxx);

// ruta POST para login de usuarios
router.post('/login', xxx);

export default router;